{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "PIO: Upload (esp32dev)",
      "type": "shell",
      "command": "pio run -e esp32dev -t upload",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "PIO: Monitor (115200)",
      "type": "shell",
      "command": "pio device monitor -b 115200",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "PIO: Upload and Monitor",
      "dependsOn": [
        "PIO: Upload (esp32dev)",
        "PIO: Monitor (115200)"
      ],
      "dependsOrder": "sequence"
    }
  ]
}

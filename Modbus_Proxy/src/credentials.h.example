#pragma once

// WiFi credentials - UPDATE THESE FOR YOUR SETUP
static const char* ssid = "YOUR_WIFI_SSID";
static const char* password = "YOUR_WIFI_PASSWORD";

// Note: MQTT settings are now configured via NVS (persistent storage)
// Default values are in nvs_config.h:
//   - MQTT Host: 192.168.0.203:1883
//   - MQTT User: admin
//   - Wallbox Topic: wallbox
//
// Configuration can be changed at runtime via MQTT:
//   Topic: MBUS-PROXY/cmd/config
//   Commands: set_mqtt, set_wallbox_topic, set_log_level, get_config, factory_reset
